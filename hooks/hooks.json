{
  "description": "Reminds Claude to append a TL;DR block to long replies (the human:tldr skill).",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo 'When a reply runs past ~12 lines or ~150 words and is not mostly code, files or a table, invoke the human:tldr skill and end that reply with its TL;DR block. Applies for the whole session.'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
